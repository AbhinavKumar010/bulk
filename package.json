{
  "name": "bulk-order-project",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install": "cd client && npm install --legacy-peer-deps",
    "build": "cd client && npm install --legacy-peer-deps && npm run build",
    "start": "cd client && npm run preview",
    "vercel-build": "cd client && npm install --legacy-peer-deps && npm run build"
  },
  "engines": {
    "node": ">=16.0.0",
    "npm": ">=7.0.0"
  }
}
